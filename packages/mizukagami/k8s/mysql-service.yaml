apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: mizukagami
  labels:
    app.kubernetes.io/name: mizukagami-mysql
spec:
  selector:
    app.kubernetes.io/name: mizukagami-mysql
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
