apiVersion: v1
kind: Service
metadata:
  name: mizukagami
  namespace: mizukagami
  labels:
    app.kubernetes.io/name: mizukagami
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mizukagami
  ports:
    - name: http
      port: 80
      targetPort: http
