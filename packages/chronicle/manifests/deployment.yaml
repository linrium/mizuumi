apiVersion: apps/v1
kind: Deployment
metadata:
  name: chronicle
  namespace: chronicle
  labels:
    app.kubernetes.io/name: chronicle
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: chronicle
  template:
    metadata:
      labels:
        app.kubernetes.io/name: chronicle
    spec:
      securityContext:
        fsGroup: 65532
      initContainers:
        - name: create-rustfs-bucket
          image: rustfs/rc:v0.1.28
          env:
            - name: RUSTFS_ENDPOINT
              value: http://rustfs-svc.storage.svc.cluster.local:9000
            - name: RUSTFS_BUCKET
              value: chronicle
            - name: RUSTFS_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: rustfs-auth
                  key: RUSTFS_ACCESS_KEY
            - name: RUSTFS_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: rustfs-auth
                  key: RUSTFS_SECRET_KEY
          command:
            - /bin/sh
            - -ec
            - |
              until rc alias set --bucket-lookup path rustfs "$RUSTFS_ENDPOINT" "$RUSTFS_ACCESS_KEY" "$RUSTFS_SECRET_KEY"; do
                sleep 2
              done
              rc bucket create --ignore-existing "rustfs/$RUSTFS_BUCKET"
      containers:
        - name: chronicle
          image: chronicle:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3000
          env:
            - name: CHRONICLE_ADDR
              value: ":3000"
            - name: CHRONICLE_LOG_DIR
              value: /var/lib/chronicle/tessera
            - name: CHRONICLE_SIGNER_KEY_FILE
              value: /var/lib/chronicle/tessera/.state/signer.key
            - name: CHRONICLE_STORAGE_BACKEND
              value: aws-s3
            - name: CHRONICLE_AWS_S3_ENDPOINT
              value: http://rustfs-svc.storage.svc.cluster.local:9000
            - name: CHRONICLE_AWS_S3_BUCKET
              value: chronicle
            - name: CHRONICLE_AWS_S3_REGION
              value: us-east-1
            - name: CHRONICLE_AWS_S3_USE_PATH_STYLE
              value: "true"
            - name: CHRONICLE_AWS_S3_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: rustfs-auth
                  key: RUSTFS_ACCESS_KEY
            - name: CHRONICLE_AWS_S3_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: rustfs-auth
                  key: RUSTFS_SECRET_KEY
            - name: CHRONICLE_AWS_MYSQL_HOST
              value: mysql.chronicle.svc.cluster.local
            - name: CHRONICLE_AWS_MYSQL_PORT
              value: "3306"
            - name: CHRONICLE_AWS_MYSQL_DATABASE
              valueFrom:
                secretKeyRef:
                  name: chronicle-mysql
                  key: MYSQL_DATABASE
            - name: CHRONICLE_AWS_MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: chronicle-mysql
                  key: MYSQL_USER
            - name: CHRONICLE_AWS_MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: chronicle-mysql
                  key: MYSQL_PASSWORD
          volumeMounts:
            - name: data
              mountPath: /var/lib/chronicle
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 2
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 65532
            runAsGroup: 65532
            capabilities:
              drop:
                - ALL
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: chronicle-data
